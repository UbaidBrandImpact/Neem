<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.bim.neem">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginReg.LoginRegActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".Home.MainActivity"></activity>
        <activity android:name=".Products.ProductsActivity"
            android:windowSoftInputMode="adjustNothing"
            ></activity>
        <activity android:name=".Products.ProductDetailActivity"></activity>
        <activity android:name=".Stories.StoriesActivity"></activity>
        <activity android:name=".Stories.StoryDetailActivity"></activity>
        <activity android:name=".Calendar.CalendarActivity"></activity>
        <activity android:name=".Video.VideoActivity"></activity>
        <activity android:name=".Video.VideoDetailActivity"></activity>

        <activity android:name=".AboutUs.AboutUsActivity"></activity>
        <activity android:name=".Profile.ProfileActivity"></activity>

    </application>

</manifest>